// @flow
import React, { Component } from 'react'
import { Row, Col } from 'antd'

type Props = {
};

export default class Personalize extends Component<Props> {
  props: Props;

  render() {
    return (
      <Row className='console-container'>
        <Col xs={24}>
        </Col>
      </Row>
    )
  }
}
